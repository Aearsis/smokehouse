/**
 * Here the value feeders rest. The feeder is a reader of the real world,
 * feeding current values to the sensors.
 * <p>
 * Currently, only the feeder from logfile is implemented. It is mainly because I don't have access
 * to the real hardware.
 */
package cz.eideo.smokehouse.common.feeder;